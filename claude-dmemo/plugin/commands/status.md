---
description: Show dMemo configuration status, network, scope, and memory count
allowed-tools: Bash(node:*)
---

Run dMemo's status script and report its output verbatim to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/status.cjs"
```

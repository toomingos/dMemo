export const REPO_URL = "https://github.com/toomingos/dMemo"
export const DOCS_URL = "https://github.com/toomingos/dMemo/tree/main/docs"
// The bare `dmemo` name on npm is permanently reserved by someone else, so
// the CLI publishes under the scope. The hero's copy button hands this
// string straight to a terminal — it has to be the command that works.
export const NPM_URL = "https://www.npmjs.com/package/@dmemo/cli"

export const INSTALL_COMMAND = "npx @dmemo/cli setup"
